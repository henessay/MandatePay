{
  "name": "mandatepay",
  "version": "0.1.0",
  "private": true,
  "description": "CFO-signed, bounded payroll delegation to an autonomous agent on Terminal 3 — zero-PII, attested, on-chain mirrored.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "dev": "pnpm --filter @mandatepay/web dev",
    "contracts:test": "cd packages/contracts && forge test -vv"
  },
  "packageManager": "pnpm@10.33.0"
}
